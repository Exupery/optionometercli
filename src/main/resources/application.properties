spring.application.name = main
spring.main.web-application-type = NONE

screener.expiration.minDays = ${MIN_DAYS:25}
screener.expiration.maxDays = ${MAX_DAYS:45}
screener.numLegs = ${TRADE_TYPES:2,3,4}

marketdata.rootEndpoint = https://api.marketdata.app
marketdata.maxStrikes = ${MAX_STRIKES:20}
marketdata.fridayOnly = ${FRIDAY_ONLY:false}
